spring.config.import: classpath:acme.oms.yaml
spring.temporal:
  namespace: ${TEMPORAL_APPS_NAMESPACE:apps}
  connection:
    target: ${TEMPORAL_APPS_ADDRESS:localhost:7233}
    api-key: ${TEMPORAL_APPS_API_KEY:}
  workers:
    - task-queue: apps
      workflow-classes:
        - com.acme.apps.workflows.v1.OrderImpl
      activity-beans:
        - options-activities
