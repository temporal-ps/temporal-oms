apiVersion: apps/v1
kind: Deployment
metadata:
  name: apps-worker
  namespace: temporal-oms-apps
  labels:
    app: apps-worker
    component: worker
    bounded-context: apps
    oms-build-id: "local"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apps-worker
  template:
    metadata:
      labels:
        app: apps-worker
        component: worker
        version: "1.0.0"
        bounded-context: apps
        oms-build-id: "local"
      annotations:
        instrumentation.opentelemetry.io/inject-sdk: "true"
    spec:
      containers:
      - name: worker
        image: temporal-oms/apps-worker:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 9092
          name: management
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: k8s
        envFrom:
        - configMapRef:
            name: temporal-oms-config
        - secretRef:
            name: temporal-oms-secrets
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 9092
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 9092
          initialDelaySeconds: 20
          periodSeconds: 5
        volumeMounts:
        - name: temporal-config
          mountPath: /etc/config/temporal
          readOnly: true
        - name: temporal-secret
          mountPath: /etc/config/temporal-secret
          readOnly: true
        resources:
          requests:
            memory: "384Mi"
            cpu: "200m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
      volumes:
      - name: temporal-config
        configMap:
          name: temporal-apps-config
          defaultMode: 0444
      - name: temporal-secret
        secret:
          secretName: temporal-apps-api-key
          defaultMode: 0444
---
apiVersion: v1
kind: Service
metadata:
  name: apps-worker
  namespace: temporal-oms-apps
  labels:
    app: apps-worker
spec:
  type: ClusterIP
  selector:
    app: apps-worker
  ports:
  - port: 9092
    targetPort: 9092
    protocol: TCP
    name: management
