apiVersion: apps/v1
kind: Deployment
metadata:
  name: processing-workers
  namespace: temporal-oms-processing
  labels:
    app: processing-workers
    component: worker
    bounded-context: processing
    oms-build-id: "local"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: processing-workers
  template:
    metadata:
      labels:
        app: processing-workers
        component: worker
        version: "1.0.0"
        bounded-context: processing
        oms-build-id: "local"
      annotations:
        instrumentation.opentelemetry.io/inject-sdk: "true"
    spec:
      containers:
      - name: worker
        image: temporal-oms/processing-workers:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 9082
          name: management
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: k8s,unversioned
        - name: JAVA_OPTS
          value: "-Dgrpc.netty.keepaliveTime=30 -Dgrpc.netty.keepaliveTimeout=5 -Dgrpc.netty.keepaliveWithoutCalls=true"
        envFrom:
        - configMapRef:
            name: temporal-oms-config
        - secretRef:
            name: temporal-oms-secrets
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 9082
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 9082
          initialDelaySeconds: 20
          periodSeconds: 5
        volumeMounts:
        - name: temporal-config
          mountPath: /etc/config/temporal
          readOnly: true
        - name: temporal-secret
          mountPath: /etc/config/temporal-secret
          readOnly: true
        resources:
          requests:
            memory: "384Mi"
            cpu: "200m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
      volumes:
      - name: temporal-config
        configMap:
          name: temporal-processing-config
          defaultMode: 0444
      - name: temporal-secret
        secret:
          secretName: temporal-processing-api-key
          defaultMode: 0444
---
apiVersion: v1
kind: Service
metadata:
  name: processing-workers
  namespace: temporal-oms-processing
  labels:
    app: processing-workers
spec:
  type: ClusterIP
  selector:
    app: processing-workers
  ports:
  - port: 9082
    targetPort: 9082
    protocol: TCP
    name: management
