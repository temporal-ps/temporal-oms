version: v2
clean: true
managed:
  enabled: true
  override:
    - file_option: java_package_prefix
      value: com.acme.proto
    - file_option: java_multiple_files
      value: true
plugins:
  # Java generation: pinned to match the protobuf-java runtime version
  # pinned in java/pom.xml's protobuf.version (4.34.1). An unpinned remote
  # plugin resolves to the BSR's current latest and can outrun the runtime,
  # which fails at class-init with a Gencode/Runtime version mismatch.
  - remote: buf.build/protocolbuffers/java:v34.1
    out: java/generated/src/main/java

  # Python generation — pinned to match installed protobuf runtime (6.33.x / C++ 33.x)
  - remote: buf.build/protocolbuffers/python:v33.0
    out: python/generated

  # Python type stubs
  - remote: buf.build/protocolbuffers/pyi:v33.0
    out: python/generated

  # Generate Pydantic models from .proto via protobuf-to-pydantic plugin
  # Binary lives in the python/ uv venv — path is relative to repo root
  - local: python/.venv/bin/protoc-gen-protobuf-to-pydantic
    out: python/generated/pydantic

  # TypeScript generation
  - remote: buf.build/bufbuild/es:v2.10.2
    out: web/src/lib/generated
